# -*- mode: ruby -*-

$project = {
  "project"         => "VAGRANT-WP",

  "apache_version"  => "2.4.*",
  "php_version"     => "5.5.*",
  "mysql_version"   => "5.5.*",
}

require("./.vagrant/config/vagrant.rb")